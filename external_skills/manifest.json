{
  "skills": [
    {
      "name": "analyze_sentiment",
      "module": "sentiment_analyzer",
      "description": "分析给定文本的情感倾向性 (极性比例)。",
      "parameters": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string",
            "description": "待分析的快讯或文本内容"
          }
        },
        "required": ["text"]
      }
    },
    {
      "name": "plan_agentic_workflow",
      "module": "superpowers_workflow",
      "description": "运用 Superpowers 方法论针对目标任务自动拆解规划、隔离、TDD、验证与复盘工作流方案。",
      "parameters": {
        "type": "object",
        "properties": {
          "task_description": {
            "type": "string",
            "description": "待分析和规划的目标任务描述"
          }
        },
        "required": ["task_description"]
      }
    },
    {
      "name": "scan_agent_skills",
      "module": "skillspector_shield",
      "description": "用于在运行前对外部加载的技能代码实施恶意模式与漏洞安全审查。",
      "parameters": {
        "type": "object",
        "properties": {
          "target_path": {
            "type": "string",
            "description": "需要被扫描的目标文件夹或文件路径，默认为 './external_skills'"
          }
        },
        "required": []
      }
    }
  ]
}
